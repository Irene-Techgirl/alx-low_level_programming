#include "main.h"

/**
 * print_most_numbers - print num except 2 and 4
 * return: void
 */
void print_most_numbers(void)
{
	char c

	for (C = '0'; c <= '9; C++)
	{
		if (I(c == '2' (l c == '49))
			_putchar(c);
	}	
	_putchar('\n');
}
